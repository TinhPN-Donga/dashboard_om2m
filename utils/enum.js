const StatusEnum = {
    CREATED: 'created',
    PENDING: 'pending',
    COMPLETED: 'completed',
    CANCELED: 'canceled',
  };

const RoleUserEnum = {
    USER: 'user',
    ADMIN: 'admin',
  };

module.exports = {
    StatusEnum,
    RoleUserEnum
}