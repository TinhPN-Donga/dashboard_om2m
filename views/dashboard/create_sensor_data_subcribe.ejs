<%- include('includes/header') %>
    <div class="page-wrapper">
        <div class="page-breadcrumb bg-white">
            <div class="row align-items-center">
                <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                    <h4 class="page-title">Create New Sensor in Tool</h4>
                </div>
            </div>
        </div>
        <div class="container-fluid">
            <form action="" method="POST">
                <div class="form-group">
                  <label for="titleSensor">Title</label>
                  <input type="text" class="form-control" id="titleSensor" name="title" aria-describedby="titleSensor" placeholder="Title">
                  <!-- <small id="title" class="form-text text-muted">Create ...</small> -->
                </div>
                <div class="form-group">
                  <label for="titleSensor">Add url API send Notify</label>
                  <input type="text" class="form-control" id="urlAPI" name="urlAPI" aria-describedby="urlAPI" placeholder="Enter URL API">
                  <small id="title" class="form-text text-muted">Create ...</small>
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
              </form>
        </div>
    </div>
    <script>
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('error')) {
          alert(`error: ${urlParams.get('error')}`);
        }
    </script>
<%- include('includes/footer') %>